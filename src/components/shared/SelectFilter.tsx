
interface SelectFilterProps {
    paramName: string;
    placeholder?: string;
    options: { label: string; value: string }[];
}

const SelectFilter = ({
    paramName,
    placeholder,
    options,
}: SelectFilterProps) => {
    return (
        <div>

        </div>
    );
};

export default SelectFilter;